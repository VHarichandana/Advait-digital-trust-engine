{
  "name": "backend",
  "version": "1.0.0",
  "description": "1. create virtualenv: python -m venv venv\r 2. pip install -r requirements.txt\r 3. copy .env.example -> .env and edit if needed\r 4. run: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  }
}
